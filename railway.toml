# Deploy config for The True-Crime Detective.
# Production runs the FastAPI webhook app; Railway injects PORT and fastapi reads it.
[deploy]
startCommand = "fastapi run src/agent/agents/detective/app.py"
